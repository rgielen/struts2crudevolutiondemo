<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>

    <constant name="struts.devMode" value="true"/>
    <constant name="struts.convention.classes.reload" value="false" />

    <package name="demo-default" extends="struts-default">

        <interceptors>
            <interceptor name="transactionInterceptor"
                         class="net.itneering.demo.struts2evolution.interceptor.TransactionalSessionInterceptor"/>

            <interceptor-stack name="defaultDemoStack">
                <interceptor-ref name="transactionInterceptor"/>
                <interceptor-ref name="defaultStack"/>
            </interceptor-stack>
        </interceptors>

        <default-interceptor-ref name="defaultDemoStack"/>

        <action name="menu">
            <result>/WEB-INF/content/menu.jsp</result>
        </action>

    </package>

    <constant name="struts.convention.default.parent.package" value="demo-default"/>

    <!--

    <package name="crudDefault" extends="default">
        <default-interceptor-ref name="paramsPrepareParamsStack"/>
    </package>

    <package name="empoyeeManager" namespace="/" extends="crudDefault">

        <action name="edit*" class="net.itneering.demo.struts2evolution.action.{1}Action" method="edit">
            <result>/WEB-INF/jsp/{1}/edit{1}.jsp</result>
        </action>

        <action name="save*" class="net.itneering.demo.struts2evolution.action.{1}Action" method="save">
            <result name="success" type="redirectAction" >
                <param name="actionName">edit{1}</param>
                <param name="model.id">${id}</param>
            </result>
            <result name="input">/WEB-INF/jsp/{1}/edit{1}.jsp</result>
        </action>

    </package>
-->


</struts>
